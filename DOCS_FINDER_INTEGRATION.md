# Интеграция API Documentation Finder

## Описание

Интегрирован модуль автоматического поиска документации API из проекта [api_docs_finder](https://github.com/lamantinX/api_docs_finder). Теперь при обнаружении невалидного ответа от URL документации система автоматически пытается найти новую актуальную ссылку.

## Как это работает

### 1. Проверка валидности ответа

При получении контента от URL система проверяет:
- Длину ответа (минимум 100 символов)
- Наличие индикаторов ошибок (404, 500, "not found", "error" и т.д.)
- Корректность структуры данных

### 2. Автоматический поиск новой документации

Если ответ невалидный, запускается двухэтапный поиск:

#### Этап 1: Прямой поиск OpenAPI
Проверяются стандартные пути для OpenAPI/Swagger документации:
- `/openapi.json`, `/openapi.yaml`
- `/swagger.json`, `/swagger.yaml`
- `/api-docs`, `/api-docs.json`
- `/v1/openapi.json`, `/v2/openapi.json`, `/v3/openapi.json`
- `/docs/openapi.json`, `/api/openapi.json`
- `/redoc`, `/swagger`, `/swagger-ui`

#### Этап 2: Поиск через SerpAPI (если настроен)
Если прямой поиск не дал результатов, выполняется поиск через Google:
- Формируется запрос: `{api_name} API documentation {method_name}`
- Используется SerpAPI для получения результатов
- Возвращается первый релевантный результат

### 3. Уведомление об обновлении

При успешном нахождении новой документации:
- Отправляется уведомление в Slack (если настроен)
- Логируется информация о старом и новом URL
- Система продолжает работу с новым URL

## Настройка

### 1. Установка зависимостей

```bash
cd api_watcher
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Добавьте в `.env` файл:

```bash
# SerpAPI (опционально, для расширенного поиска)
SERPAPI_KEY=your_serpapi_key_here
```

Получить ключ можно на https://serpapi.com/

### 3. Проверка конфигурации

```python
from api_watcher.config import Config

# Проверяем, настроен ли SerpAPI
if Config.is_serpapi_configured():
    print("✅ SerpAPI настроен")
else:
    print("⚠️ SerpAPI не настроен - будет использоваться только прямой поиск OpenAPI")
```

## Использование

### Автоматический режим

Интеграция работает автоматически при запуске watcher_v2:

```bash
python -m api_watcher.watcher_v2
```

При обнаружении невалидного ответа система:
1. Логирует предупреждение
2. Запускает поиск новой документации
3. Пытается получить контент с нового URL
4. Отправляет уведомление в Slack
5. Продолжает мониторинг с новым URL

### Программное использование

```python
from api_watcher.utils.docs_finder import find_api_documentation

# Поиск документации
docs_info = await find_api_documentation(
    url="https://api.example.com/old-docs",
    api_name="Example API",
    method_name="Get Users",
    serpapi_key="your_key"  # опционально
)

if docs_info:
    print(f"Найдена документация: {docs_info['url']}")
    print(f"Тип: {docs_info['type']}")  # 'openapi' или 'search'
```

## Примеры работы

### Пример 1: Успешный прямой поиск OpenAPI

```
🔍 Обработка: Stripe API
🌐 Получаем контент через ZenRows: https://stripe.com/docs/api/old-endpoint
⚠️ Невалидный ответ от URL, пытаемся найти новую документацию
🔍 Поиск OpenAPI документации для https://stripe.com
✅ Найдена OpenAPI документация: https://stripe.com/openapi.json
✅ Успешно получен контент с нового URL
```

### Пример 2: Поиск через SerpAPI

```
🔍 Обработка: Twilio API
🌐 Получаем контент напрямую: https://twilio.com/docs/old-api
⚠️ Обнаружен индикатор ошибки: 404
⚠️ Невалидный ответ от URL, пытаемся найти новую документацию
🔍 Поиск OpenAPI документации для https://twilio.com
❌ OpenAPI документация не найдена
🔍 Поиск через SerpAPI: Twilio API documentation
✅ Найдена новая документация (search): https://www.twilio.com/docs/usage/api
```

### Пример 3: Slack уведомление

```
🔄 Обновлена ссылка на документацию

API: Stripe API
Метод: Create Payment Intent
Старый URL: https://stripe.com/docs/api/old-endpoint
Новый URL: https://stripe.com/openapi.json
Тип: openapi
```

## Логика обработки ошибок

```
┌─────────────────────────┐
│  Получение контента     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Проверка валидности    │
└───────────┬─────────────┘
            │
            ├─── Валидный ──────────► Продолжить обработку
            │
            └─── Невалидный
                    │
                    ▼
        ┌─────────────────────────┐
        │  Прямой поиск OpenAPI   │
        └───────────┬─────────────┘
                    │
                    ├─── Найдено ──────► Получить контент
                    │                    │
                    │                    ├─── Валидный ──► Продолжить
                    │                    └─── Невалидный ─► Ошибка
                    │
                    └─── Не найдено
                            │
                            ▼
                ┌─────────────────────────┐
                │  Поиск через SerpAPI    │
                └───────────┬─────────────┘
                            │
                            ├─── Найдено ──────► Получить контент
                            │                    │
                            │                    ├─── Валидный ──► Продолжить
                            │                    └─── Невалидный ─► Ошибка
                            │
                            └─── Не найдено ────► Ошибка
```

## Ограничения

1. **SerpAPI лимиты**: Бесплатный план SerpAPI имеет ограничение на количество запросов
2. **Точность поиска**: Поиск через SerpAPI может вернуть нерелевантные результаты
3. **Производительность**: Поиск документации добавляет задержку при обнаружении невалидного ответа

## Рекомендации

1. **Настройте SerpAPI**: Для лучших результатов рекомендуется настроить SerpAPI ключ
2. **Мониторинг Slack**: Настройте Slack для получения уведомлений об обновлениях URL
3. **Регулярная проверка**: Периодически проверяйте логи на наличие невалидных ответов
4. **Обновление URLs**: После получения уведомления обновите URL в конфигурации

## Отладка

Включите подробное логирование:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

Проверьте работу поиска документации:

```bash
# Тест прямого поиска OpenAPI
python -c "
import asyncio
from api_watcher.utils.docs_finder import APIDocsFinder

async def test():
    async with APIDocsFinder() as finder:
        result = await finder.find_openapi_direct('https://api.stripe.com')
        print(f'Result: {result}')

asyncio.run(test())
"
```

## Поддержка

При возникновении проблем:
1. Проверьте логи в `api_watcher.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте валидность SerpAPI ключа (если используется)
4. Проверьте доступность целевых URL
