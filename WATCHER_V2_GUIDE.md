# API Watcher V2 - Руководство

## 🚀 Новые возможности

API Watcher V2 - это улучшенная версия с продвинутыми функциями:

### ✨ Основные улучшения

1. **База данных (SQLite/PostgreSQL)**
   - Хранение полной истории HTML-снэпшотов
   - Метаданные и временные метки
   - Быстрый поиск и анализ истории

2. **ZenRows интеграция**
   - Обход защиты сайтов (Cloudflare, reCAPTCHA и т.д.)
   - JavaScript рендеринг
   - Премиум прокси для сложных случаев

3. **Gemini AI анализ**
   - Умное определение существенных изменений
   - Автоматические сводки изменений на русском языке
   - Игнорирование мелких правок и опечаток

4. **Slack уведомления**
   - Красивые форматированные сообщения
   - Уровни важности (minor/moderate/major)
   - Еженедельные дайджесты

5. **Умное сравнение**
   - Структурное сравнение OpenAPI без AI (быстро)
   - AI анализ только при обнаружении изменений
   - Категоризация изменений (новые endpoints, breaking changes и т.д.)

## 📦 Установка

### 1. Установите зависимости

```bash
cd api_watcher
pip install -r requirements.txt
```

### 2. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните необходимые ключи:

```env
# Обязательные для базовой работы
DATABASE_URL=sqlite:///api_watcher.db

# Опциональные (но рекомендуемые)
ZENROWS_API_KEY=your_key_here
GEMINI_API_KEY=your_key_here
SLACK_BOT_TOKEN=xoxb-your-token
SLACK_CHANNEL=#api-changes
```

### 3. Получите API ключи

#### ZenRows (рекомендуется)
1. Зарегистрируйтесь на https://www.zenrows.com/
2. Получите API ключ в dashboard
3. Бесплатный план: 1000 запросов/месяц

#### Google Gemini AI (рекомендуется)
1. Перейдите на https://makersuite.google.com/app/apikey
2. Создайте API ключ
3. Бесплатный план: 60 запросов/минуту

#### Slack (опционально)
1. Создайте Slack App: https://api.slack.com/apps
2. Добавьте Bot Token Scopes: `chat:write`, `chat:write.public`
3. Установите приложение в workspace
4. Скопируйте Bot User OAuth Token

## 🎯 Использование

### Разовая проверка

```bash
python -m api_watcher.watcher_v2
```

### Еженедельная проверка (рекомендуется)

```bash
python api_watcher/weekly_check.py
```

### Настройка cron (Linux/Mac)

Добавьте в crontab для проверки каждое воскресенье в 10:00:

```bash
crontab -e
```

```cron
0 10 * * 0 cd /path/to/api_watcher && python api_watcher/weekly_check.py
```

### Настройка Task Scheduler (Windows)

1. Откройте Task Scheduler
2. Создайте новую задачу
3. Триггер: Еженедельно, воскресенье, 10:00
4. Действие: Запуск программы
   - Программа: `python`
   - Аргументы: `api_watcher/weekly_check.py`
   - Рабочая папка: путь к проекту

## 📊 Как это работает

### Workflow для HTML контента

```
1. Получение контента через ZenRows
   ↓
2. Конвертация HTML → текст
   ↓
3. Быстрое сравнение по хешу
   ↓
4. Если изменения → AI анализ через Gemini
   ↓
5. AI определяет: существенные или нет?
   ↓
6. Если существенные → сохранение в БД + уведомление в Slack
```

### Workflow для OpenAPI

```
1. Получение спецификации
   ↓
2. Структурное сравнение (DeepDiff)
   ↓
3. Если изменения → категоризация
   ↓
4. AI создает человекочитаемую сводку
   ↓
5. Сохранение в БД + уведомление в Slack
```

## 🔧 Конфигурация

### Формат urls.json

```json
[
  {
    "url": "https://api.example.com/docs",
    "api_name": "Example API",
    "method_name": "Get Users"
  },
  {
    "url": "https://api.example.com/openapi.json",
    "api_name": "Example API",
    "method_name": "OpenAPI Spec"
  }
]
```

### Настройка интервала проверки

В `.env`:

```env
CHECK_INTERVAL_DAYS=7  # Проверять раз в неделю
```

### Выбор модели Gemini

```env
GEMINI_MODEL=gemini-pro  # Базовая модель (рекомендуется)
# GEMINI_MODEL=gemini-pro-vision  # Для анализа изображений
```

## 📈 База данных

### Структура таблицы snapshots

```sql
CREATE TABLE snapshots (
    id INTEGER PRIMARY KEY,
    url VARCHAR(500),
    api_name VARCHAR(200),
    method_name VARCHAR(200),
    content_type VARCHAR(50),
    raw_html TEXT,
    text_content TEXT,
    structured_data TEXT,
    created_at DATETIME,
    has_changes BOOLEAN,
    ai_summary TEXT,
    content_hash VARCHAR(64)
);
```

### Просмотр истории

```python
from api_watcher.storage.database import DatabaseManager

db = DatabaseManager('sqlite:///api_watcher.db')

# Получить последний снэпшот
snapshot = db.get_latest_snapshot('https://api.example.com/docs')

# Получить историю
history = db.get_snapshot_history('https://api.example.com/docs', limit=10)

# Получить все изменения за неделю
changes = db.get_snapshots_with_changes(days=7)
```

## 🎨 Примеры Slack уведомлений

### Уведомление об изменении

```
🟡 Изменения в API: HubSpot API - Create Contact

Сводка изменений:
Добавлено новое обязательное поле 'company_id' в запрос создания контакта.
Изменен формат ответа - теперь включает дополнительные метаданные.

API: HubSpot API
Важность: MODERATE

Ключевые изменения:
• Добавлено обязательное поле company_id
• Изменен формат ответа
• Обновлена документация по ошибкам

[Открыть документацию]
```

### Еженедельная сводка

```
📊 Еженедельная сводка изменений API

Обнаружено изменений: 5

---

HubSpot API - Create Contact
Добавлено новое обязательное поле 'company_id'...
[Открыть]

Stripe API - Payment Intent
Изменены лимиты rate limiting...
[Открыть]

...
```

## 🔍 Отладка

### Включить подробное логирование

```env
API_WATCHER_LOG_LEVEL=DEBUG
```

### Проверить конфигурацию

```python
from api_watcher.config import Config

print(f"ZenRows: {Config.is_zenrows_configured()}")
print(f"Gemini: {Config.is_gemini_configured()}")
print(f"Slack: {Config.is_slack_configured()}")
```

### Тестовый запуск без уведомлений

Временно закомментируйте в `.env`:

```env
# SLACK_BOT_TOKEN=...
# SLACK_CHANNEL=...
```

## 💡 Советы по оптимизации

### 1. Используйте ZenRows только для сложных сайтов

Для простых API можно отключить ZenRows:

```env
# ZENROWS_API_KEY=  # Закомментируйте
```

### 2. Настройте игнорирование путей для OpenAPI

В коде можно добавить пути для игнорирования:

```python
ignore_paths = [
    "root['info']['version']",
    "root['servers']",
    "root['externalDocs']"
]
```

### 3. Используйте PostgreSQL для больших объемов

```env
DATABASE_URL=postgresql://user:password@localhost/api_watcher
```

## 🆚 Сравнение V1 vs V2

| Функция | V1 | V2 |
|---------|----|----|
| Хранение | Файлы JSON | База данных |
| Получение контента | requests | ZenRows + fallback |
| Анализ изменений | DeepDiff | DeepDiff + Gemini AI |
| Уведомления | Telegram | Telegram + Slack |
| OpenAPI сравнение | Базовое | Структурное + AI сводка |
| История | Нет | Полная история в БД |
| Еженедельные дайджесты | Нет | Да |

## 🤝 Миграция с V1 на V2

1. Установите новые зависимости
2. Настройте `.env` с новыми переменными
3. Первый запуск создаст БД и сохранит текущие снэпшоты
4. Старые файлы в `snapshots/` можно оставить как backup

## 📝 Лицензия

MIT License - используйте свободно!
