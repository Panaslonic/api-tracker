# 🚀 Быстрое руководство по тестированию

## Установка и запуск

### 1. Установите зависимости
```bash
cd api_watcher
pip install -r requirements.txt
```

### 2. Запустите тесты
```bash
# Все тесты
python run_tests.py

# Быстрые тесты
python run_tests.py quick

# Показать справку
python run_tests.py help
```

## Основные команды

| Команда | Описание |
|---------|----------|
| `python run_tests.py` | Все тесты |
| `python run_tests.py unit` | Юнит тесты |
| `python run_tests.py integration` | Интеграционные тесты |
| `python run_tests.py coverage` | Тесты с покрытием |
| `python run_tests.py quick` | Быстрые тесты |

## Makefile команды

| Команда | Описание |
|---------|----------|
| `make test` | Все тесты |
| `make test-unit` | Юнит тесты |
| `make test-coverage` | С покрытием кода |
| `make help` | Показать все команды |

## Структура тестов

```
tests/
├── test_config.py      # Тесты конфигурации
├── test_main.py        # Тесты основного модуля  
├── test_parsers.py     # Тесты парсеров
├── test_storage.py     # Тесты хранилища
├── test_notifiers.py   # Тесты уведомителей
├── test_utils.py       # Тесты утилит
└── test_integration.py # Интеграционные тесты
```

## Что тестируется

✅ **Конфигурация** - переменные окружения, настройки  
✅ **Парсеры** - JSON, HTML, OpenAPI, Postman, Markdown  
✅ **Хранилище** - сохранение и загрузка снимков  
✅ **Уведомления** - консоль и Telegram  
✅ **Сравнение** - обнаружение изменений  
✅ **Интеграция** - полный рабочий процесс  

## Быстрая проверка

```bash
# Проверить, что все работает
python -c "
import sys
sys.path.append('.')
from tests.test_config import TestConfig
t = TestConfig()
t.test_default_values()
print('✅ Тесты работают!')
"
```

## Отчеты

После запуска `python run_tests.py coverage`:
- HTML отчет: `htmlcov/index.html`
- Терминальный отчет в консоли

## Troubleshooting

**Проблема:** ModuleNotFoundError  
**Решение:** Убедитесь, что находитесь в папке `api_watcher`

**Проблема:** Тесты не запускаются  
**Решение:** Установите зависимости: `pip install -r requirements.txt`

**Проблема:** Ошибки импорта  
**Решение:** Проверьте структуру файлов и PYTHONPATH

---

**Готово!** Теперь у вас есть полный набор тестов для API Watcher 🎉