# API Watcher

Микросервис для мониторинга изменений в API документации различных форматов.

## Возможности

- 🔍 Мониторинг HTML-страниц документации
- 📋 Парсинг OpenAPI спецификаций (JSON/YAML)
- 📄 Анализ произвольных JSON Schema
- 📮 Обработка Postman коллекций
- 📝 Парсинг Markdown документации
- 💾 Сохранение снимков состояния
- 🔄 Детальное сравнение изменений через DeepDiff
- 📢 Уведомления в консоль и Telegram

## Установка

1. Клонируйте репозиторий или скопируйте файлы проекта
2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Windows
   source venv/bin/activate  # Linux/Mac
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

## Настройка

### Основная конфигурация

Отредактируйте файл `urls.json` для добавления источников мониторинга:

```json
[
  {
    "url": "https://docs.stripe.com/api/customers#create_customer",
    "type": "html",
    "name": "Stripe - Создание клиента",
    "selector": ".method",
    "description": "Мониторинг метода создания клиента"
  },
  {
    "url": "https://petstore3.swagger.io/api/v3/openapi.json",
    "type": "openapi",
    "name": "Swagger Petstore - Pets",
    "method_filter": "/pet",
    "description": "Мониторинг методов работы с питомцами"
  },
  {
    "url": "https://api.github.com",
    "type": "json",
    "name": "GitHub API Root",
    "description": "Мониторинг корневого эндпоинта"
  }
]
```

### Дополнительные параметры конфигурации

- **name** - Человекочитаемое название источника
- **description** - Описание того, что мониторится
- **selector** - CSS селектор для HTML (например, `.method`, `#api-section`)
- **method_filter** - Фильтр путей для OpenAPI (например, `/users`, `/orders`)

### Поддерживаемые типы документации

- **html** - HTML-страницы с поддержкой селекторов и якорей
  - Автоматический поиск API-секций
  - Поддержка CSS селекторов (`selector: ".method"`)
  - Поиск по якорям (`#method-name`)
  - Извлечение заголовков, кода, параметров, примеров
- **openapi** - OpenAPI спецификации в JSON/YAML формате
  - Фильтрация по методам (`method_filter: "/pet"`)
  - Извлечение путей, параметров, схем
  - Поддержка requestBody и responses
- **json** - Произвольные JSON Schema и API эндпоинты
  - Анализ структуры данных
  - Поддержка локальных файлов
  - Сравнение ключей и значений
- **postman** - Postman коллекции
  - Извлечение запросов и папок
  - Анализ методов, URL, заголовков
- **md** - Markdown документация
  - Парсинг заголовков и секций
  - Извлечение блоков кода и ссылок

### Telegram уведомления (опционально)

Для настройки Telegram уведомлений:

1. Создайте бота через @BotFather
2. Получите токен бота и chat_id
3. Модифицируйте `main.py` для использования `TelegramNotifier`:

```python
from notifier.telegram_notifier import TelegramNotifier

# В классе APIWatcher
self.telegram_notifier = TelegramNotifier(
    bot_token="YOUR_BOT_TOKEN",
    chat_id="YOUR_CHAT_ID"
)

# В методе process_url при обнаружении изменений
if diff:
    self.notifier.notify_changes(url, diff)
    self.telegram_notifier.notify_changes(url, diff)  # Добавить эту строку
```

## Запуск

### Разовый запуск
```bash
python main.py
```

### Запуск по расписанию (cron)

Для Linux/Mac добавьте в crontab:
```bash
# Каждые 30 минут
*/30 * * * * cd /path/to/api_watcher && python main.py

# Каждый час
0 * * * * cd /path/to/api_watcher && python main.py
```

Для Windows используйте Task Scheduler или PowerShell:
```powershell
# Создание задачи каждые 30 минут
$action = New-ScheduledTaskAction -Execute "python" -Argument "main.py" -WorkingDirectory "C:\path\to\api_watcher"
$trigger = New-ScheduledTaskTrigger -RepetitionInterval (New-TimeSpan -Minutes 30) -Once -At (Get-Date)
Register-ScheduledTask -TaskName "APIWatcher" -Action $action -Trigger $trigger
```

## Структура проекта

```
api_watcher/
├── main.py                    # Точка входа
├── parsers/                   # Парсеры для разных типов документации
│   ├── html_parser.py        # HTML-страницы
│   ├── openapi_parser.py     # OpenAPI спецификации
│   ├── json_parser.py        # JSON Schema
│   ├── postman_parser.py     # Postman коллекции
│   └── md_parser.py          # Markdown документы
├── storage/
│   └── snapshot_manager.py   # Управление снимками
├── notifier/
│   ├── console_notifier.py   # Уведомления в консоль
│   └── telegram_notifier.py  # Telegram уведомления
├── utils/
│   └── comparator.py         # Сравнение данных
├── snapshots/                # Директория для снимков (создается автоматически)
├── urls.json                 # Конфигурация источников
├── requirements.txt          # Зависимости
└── README.md                # Документация
```

## Добавление новых источников

1. Откройте `urls.json`
2. Добавьте новый объект с URL и типом:
   ```json
   {
     "url": "https://example.com/api/docs",
     "type": "html"
   }
   ```
3. Запустите `python main.py` для создания первого снимка

## Примеры использования

### Мониторинг API изменений
```bash
# Первый запуск - создание снимков
python main.py

# Последующие запуски - сравнение с предыдущими снимками
python main.py
```

### Тестирование Telegram уведомлений
```python
from notifier.telegram_notifier import TelegramNotifier

notifier = TelegramNotifier("BOT_TOKEN", "CHAT_ID")
notifier.test_connection()
```

## Логи и отладка

Все сообщения выводятся в консоль с цветовой индикацией:
- 🟢 Успешные операции
- 🟡 Обнаруженные изменения  
- 🔴 Ошибки
- 🔵 Информационные сообщения

## Требования

- Python 3.11+
- Интернет-соединение для доступа к источникам
- Права на запись в директорию проекта (для снимков)

## Примеры вывода

### Первый запуск (создание снимков)
```
🚀 Запуск API Watcher...
Обрабатываем: https://docs.stripe.com/api/customers (тип: html)
💾 Создан первый snapshot для https://docs.stripe.com/api/customers
✨ Обработка завершена
```

### Обнаружение изменений
```
🔔 ОБНАРУЖЕНЫ ИЗМЕНЕНИЯ
URL: https://api.example.com/schema.json
Время: 2024-10-24 15:30:45
============================================================
➕ ДОБАВЛЕНО:
  + new_endpoint
  + new_feature
🔄 ИЗМЕНЕНО:
  📍 version
    Было: 1.0.0
    Стало: 1.1.0
============================================================
```

## Расширение функциональности

### Добавление нового типа парсера

1. Создайте новый файл в `parsers/`
2. Реализуйте класс с методом `parse(url: str) -> Dict[str, Any]`
3. Добавьте парсер в `main.py`:

```python
self.parsers['new_type'] = NewTypeParser()
```

### Добавление нового уведомителя

1. Создайте класс в `notifier/`
2. Реализуйте методы `notify_changes()` и `notify_error()`
3. Добавьте в `main.py` при обнаружении изменений

## Устранение неполадок

### Ошибки сети
- Проверьте доступность URL
- Убедитесь в корректности SSL-сертификатов
- Проверьте настройки прокси

### Ошибки парсинга
- Убедитесь в корректности формата документации
- Проверьте кодировку файлов
- Проверьте права доступа к локальным файлам

### Проблемы со снимками
- Проверьте права записи в директорию `snapshots/`
- Убедитесь в наличии свободного места на диске

## Лицензия

MIT License